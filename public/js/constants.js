//---------------
// SYSTEM VALUES
//---------------

var STAGE_W = Math.max(document.documentElement.clientWidth, window.innerWidth || 600),
    STAGE_H = Math.max(document.documentElement.clientHeight, window.innerHeight || 400),
    PHYSICS = 15,
    UPDATE  = 45,
    GAME_FONT = "bold 20px sans-serif";

//var
